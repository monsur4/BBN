package com.mon.bbn.data

data class Contestant(var name:String, var age:Int, var sex: Sex, var popularity:Popularity,
                      var status:Status, var highlight:String ) {
// TODO: ("add boolean favorite")
}